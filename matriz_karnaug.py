# defino la matriz para el mapa
import numpy as np  #usamos un "alias" para no escribir numpy cada vez
#definimos una matriz
k= np.zeros((2,4)) #creamos una matriz de ceros de 2x4 para almacenar los resultados, son arreglos
#llenamos la matriz con los valores de la función, 1 para verdadero y 0 para falso
k[0][0]=1
k[1][1]=1
k[1][2]=1
k=np.
print(f'0 {k[0]}')
print(f'1 {k[1]} \n')
funcion=""

if k[0][0]==1:
    funcion=funcion+"a'b'c'"
if k[0][1]==1:
    funcion=funcion+"a'b'c'"
if k[0][2]==1:
    funcion=funcion+"a'b'c'"
if k[0][3]==1:
    funcion=funcion+"a'b'c'"
if k[1][0]==1:
    funcion=funcion+"a'b'c'"
if k[1][1]==1:
    funcion=funcion+"a'b'c'"
if k[1][2]==1:
    funcion=funcion+"a'b'c'"
if  k[1][3]==1:
    funcion=funcion+"a'b'c'"

